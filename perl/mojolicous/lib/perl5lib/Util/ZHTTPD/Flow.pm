package Util::ZHTTPD::Flow;

use Util::ZHTTPD::Config qw/$server_cfg $hc/;
use Data::Dump;
use HTTP::Status;
use URI;
use IO::File;
use POE;

sub riskagt {
}

sub risk {

  my( $req, $res, $dir ) = @_[ ARG0 .. ARG2 ]

}

1;

